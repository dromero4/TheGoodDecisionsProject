Table names and columns in a snake_case

Don't mix plural and singular names for table names

Always use vendor prefix for vendor-specific tables

Don't use special characters in directory names like "F&R"

Only English in code and comments

It would be a long story, but I recommend not using ORM at all, or at least using it with limits. 
And use a repository pattern instead of ORM. But for now we can skip it to get results faster.

Use linter and the same code formating rules. Crucial for teamwork.

Add robots.txt and sitemap.xml

Avoid links to external sites if it is possible (like with images). It can affect SEO.

Add GA (Google Analytics), take into account that it is necessary to add GDPR compliance.